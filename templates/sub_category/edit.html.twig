{% extends 'base.html.twig' %}

{% block title %}Edit SubCategory{% endblock %}

{% block body %}
<div class="row maincolor headline">
    <div class="col s1">
        <a class="btn-floating white mgr10"><i class="material-icons-outlined text-maincolor">segment</i></a>
    </div>
    <div class="col s7">
        <h5 class="main-title white-text">Sous-catégories</h5>
    </div>
    <div class="col s4 right-align">
        <a href="{{ path('sub_category_index') }}" class="btn right white text-maincolor"><i class="material-icons left">chevron_left</i> Retour</a>
    </div>
</div>
<div class="container mgt20">
    <div class="row">
        <div class="col s12">
    {{ include('sub_category/_form.html.twig', {'button_label': 'Enregistrer'}) }}
        </div>
    </div>
    <div class="row">
        {{ include('sub_category/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
