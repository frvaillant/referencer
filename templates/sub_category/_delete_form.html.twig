<form method="post" action="{{ path('sub_category_delete', {'id': sub_category.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cette catégorie ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
    <button class="btn red"><i class="material-icons-outlined left">delete</i> Supprimer</button>
</form>
